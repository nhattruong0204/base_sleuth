# Core
httpx>=0.27.0          # Async HTTP client (better than requests for polling)
aiosqlite>=0.20.0      # Async SQLite for dev
asyncpg>=0.29.0        # Async PostgreSQL for prod
sqlalchemy[asyncio]>=2.0
alembic>=1.13.0        # DB migrations

# Web scraping (for context resolution)
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Telegram notifications
python-telegram-bot>=21.0

# Blockchain
web3>=7.0.0            # Base RPC interaction (optional, for onchain monitoring)

# Config + utilities
pyyaml>=6.0
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0.0
structlog>=24.0.0      # Structured logging

# Rate limiting
aiolimiter>=1.1.0
