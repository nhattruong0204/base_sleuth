# Clanker Token Tracker — dependencies

# Async HTTP
httpx>=0.27,<1.0

# Database (SQLAlchemy 2.0 async)
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29             # PostgreSQL async driver (production)
aiosqlite>=0.20           # SQLite async driver (dev/testing)
alembic>=1.13             # Database schema migrations

# Config
pyyaml>=6.0
pydantic>=2.0,<3.0

# Scraping
beautifulsoup4>=4.12
lxml>=5.0                 # Fast HTML parser for BS4

# Telegram
python-telegram-bot>=21.0,<22.0

# Telegram client API (for NansenBot listener)
telethon>=1.36

# Logging
structlog>=24.0

# Minara x402 pay-as-you-go (optional; required only when minara.auth_method=x402)
# x402[httpx]>=0.6
# eth-account>=0.13

# Base chain (optional — for future WebSocket event monitoring)
# web3>=7.0
